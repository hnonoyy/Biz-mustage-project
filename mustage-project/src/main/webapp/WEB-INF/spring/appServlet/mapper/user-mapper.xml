<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hnono.mustage.persistence.UserDao">

	<insert id="insert">
		INSERT INTO MUSTAGE_USERS(
		id, password, name, nick, email, tel, role)
		VALUES(
		#{id}, #{password}, #{name},#{nick},#{email},#{tel},#{role}
		)
	</insert>

</mapper> 